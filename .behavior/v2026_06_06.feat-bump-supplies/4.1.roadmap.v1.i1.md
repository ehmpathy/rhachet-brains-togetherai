# 4.1.roadmap

## summary

replace dedicated-only models with serverless models. single phase.

## phases

### phase 1: update model catalog

**read first:** `.behavior/v2026_06_06.feat-bump-supplies/3.3.1.blueprint.product.v1.i1.md`

1. [ ] update `TogetherBrainAtomSlug` type union
   - remove 8 dedicated-only slugs
   - retain 2 serverless slugs (qwen3/235b, llama3.3/70b)
   - add 14 new serverless slugs
   - **verify**: typescript compiles

2. [ ] update `CONFIG_BY_ATOM_SLUG` map
   - remove 8 entries for dedicated-only models
   - add 14 new entries with model id, description, spec
   - update llama3.3/70b output price ($0.88 → $1.04)
   - **verify**: all slugs in type have config entries

3. [ ] update `getBrainAtomsByTogetherAI` export
   - remove 8 genBrainAtom calls
   - add 14 new genBrainAtom calls
   - **verify**: returns 16 atoms

4. [ ] update unit test assertion (10 → 16 atoms)
   - **verify**: `rhx git.repo.test --what unit --scope index.unit`

5. [ ] update integration test arrays
   - **verify**: `rhx git.repo.test --what integration --scope genBrainAtom`

