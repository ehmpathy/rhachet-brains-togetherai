# review.self: has-consistent-mechanisms

## the review

i paused. i opened the codebase. i searched for extant mechanisms.

i asked: did the junior duplicate extant functionality?

---

## extant mechanisms search

### search 1: config patterns

i searched for how models are configured in this repo:

```
src/domain.operations/atom/BrainAtom.config.ts
```

**extant pattern found:**

```ts
export const CONFIG_BY_ATOM_SLUG: Record<TogetherBrainAtomSlug, BrainAtomConfig> = {
  'together/qwen3/coder-next': {
    model: 'Qwen/Qwen3-Coder-Next',
    description: '...',
    spec: { cost: {...}, gain: {...} },
  },
  // ...
};
```

**new entries use same pattern:** yes

**verdict:** consistent. no duplication.

### search 2: type union patterns

i searched for how slug types are declared:

```
src/domain.operations/atom/BrainAtom.config.ts
```

**extant pattern found:**

```ts
export type TogetherBrainAtomSlug =
  | 'together/qwen3/coder-next'
  | 'together/llama3.3/70b'
  // ...
```

**new entries extend union:** yes

**verdict:** consistent. no duplication.

### search 3: export patterns

i searched for how atoms are exported:

```
src/contract/sdk/index.ts
```

**extant pattern found:**

```ts
export const getBrainAtomsByTogetherAI = () => [
  genBrainAtom({ slug: 'together/qwen3/coder-next' }),
  // ...
];
```

**new entries use same pattern:** yes

**verdict:** consistent. no duplication.

### search 4: test patterns

i searched for how models are tested:

```
src/domain.operations/atom/genBrainAtom.integration.test.ts
```

**extant pattern found:**

```ts
const allSlugs: TogetherBrainAtomSlug[] = [
  'together/qwen3/coder-next',
  // ...
];
```

**new entries extend array:** yes

**verdict:** consistent. no duplication.

---

## mechanism duplication check

### question 1: does codebase already have mechanism that does this?

| mechanism needed | extant mechanism? | action |
|------------------|-------------------|--------|
| model config map | CONFIG_BY_ATOM_SLUG | reuse |
| slug type union | TogetherBrainAtomSlug | extend |
| export function | getBrainAtomsByTogetherAI | extend |
| test arrays | allSlugs, toolCompatSlugs | extend |

**all mechanisms extant. no new mechanisms created.**

### question 2: do we duplicate extant utilities?

i searched for utilities that might be duplicated:

| utility | searched for | found? | duplicated? |
|---------|--------------|--------|-------------|
| price calculator | `cost` `calculate` | no | n/a |
| grade comparator | `grade` `compare` | no | n/a |
| model validator | `validate` `model` | no | n/a |
| config builder | `build` `config` | no | n/a |

**no utilities to duplicate. this repo has no utilities - just data.**

### question 3: could we reuse extant component instead?

| new component | extant alternative? | verdict |
|---------------|---------------------|---------|
| 10 config entries | n/a - unique data | no reuse possible |
| 10 slug entries | n/a - unique strings | no reuse possible |
| 10 export calls | n/a - unique slugs | no reuse possible |

**no reuse opportunity. each model is unique data.**

---

## conclusion

review for mechanism duplication:

| check | result |
|-------|--------|
| new mechanism created? | no |
| extant mechanism reused? | yes |
| utility duplicated? | no |
| component reusable? | no (unique data) |

**zero duplication.** all additions follow extant patterns exactly.

