# review.self: has-behavior-declaration-coverage

## the review

i paused. i re-opened 0.wish.md. i re-opened 1.vision.md. i re-opened 2.1.criteria.blackbox.md.

i read each line. i traced each requirement to the blueprint.

---

## wish requirements (0.wish.md)

### wish line 1: "research and append the latest models now available"

**traced to blueprint:**

blueprint section "filediff tree" shows:
```
src/domain.operations/atom/
├── [~] BrainAtom.config.ts   # add 10 entries to CONFIG_BY_ATOM_SLUG
```

10 entries = 10 new models appended.

**verdict:** covered.

### wish line 2: "specifically, focus on fast & cheap models"

**traced to blueprint:**

blueprint section "model specs" shows:

| slug | input/1M | tier |
|------|----------|------|
| together/lfm2/24b | $0.03 | ultra-cheap |
| together/gpt-oss/20b | $0.05 | ultra-cheap |
| together/qwen3.5/9b | $0.17 | cheap |
| together/gemma4/31b | $0.20 | cheap |

4 models at $0.20 or less. this is "fast & cheap."

**verdict:** covered.

### wish line 3: "as well as big & best models"

**traced to blueprint:**

blueprint section "model specs" shows:

| slug | swe-bench | tier |
|------|-----------|------|
| together/deepseek/v4-pro | 80.6% | frontier |
| together/kimi/k2.6 | 80.2% | frontier |
| together/minimax/m2.5 | 80.2% | frontier |
| together/glm/5.1 | 58.4% | frontier |

4 models at 58%+ swe-bench. deepseek and kimi match claude opus tier (80%+). this is "big & best."

**verdict:** covered.

### wish line 4: "include all origins"

**traced to blueprint:**

blueprint section "model specs" shows origins:

| origin | model |
|--------|-------|
| liquid | lfm2/24b |
| openai | gpt-oss/20b |
| qwen | qwen3.5/9b, qwen3.5/397b |
| google | gemma4/31b |
| minimax | minimax/m2.5, minimax/m2.7 |
| deepseek | deepseek/v4-pro |
| moonshot | kimi/k2.6 |
| zhipu | glm/5.1 |

8 distinct origins across 10 models.

**verdict:** covered.

---

## vision requirements (1.vision.md)

### vision section: "new models to add"

i counted models in vision vs blueprint:

| vision model | in blueprint? |
|--------------|---------------|
| together/lfm2/24b | yes |
| together/gpt-oss/20b | yes |
| together/qwen3.5/9b | yes |
| together/gemma4/31b | yes |
| together/minimax/m2.5 | yes |
| together/minimax/m2.7 | yes |
| together/qwen3.5/397b | yes |
| together/deepseek/v4-pro | yes |
| together/kimi/k2.6 | yes |
| together/glm/5.1 | yes |

10/10 models covered.

### vision section: "price update"

vision says: llama3.3/70b output $0.88 → $1.04

blueprint section "price update" shows:

| slug | field | old | new |
|------|-------|-----|-----|
| together/llama3.3/70b | output | $0.88 | $1.04 |

**verdict:** covered.

---

## criteria requirements (2.1.criteria.blackbox.md)

### usecase.1: select model by slug

criteria says:
```
given('a developer imports genBrainAtom')
  when('they pass a valid slug')
    then('they receive a configured brain atom')
```

blueprint codepath tree shows:
```
TogetherBrainAtomSlug (type union)
├── [+] 'together/deepseek/v4-pro'   # create
```

new slugs added to union = new valid slugs for genBrainAtom.

**verdict:** covered.

### usecase.2: choose model for budget constraints

criteria says:
```
given('a developer needs the cheapest option')
  then('they find lfm2/24b at $0.03/$0.12 per 1M tokens')
```

blueprint model specs shows lfm2/24b at $0.03/$0.12.

**verdict:** covered.

### usecase.3: choose model for code quality

criteria says:
```
given('a developer needs frontier code generation')
  then('they find deepseek/v4-pro at 80.6% swe-bench verified')
```

blueprint model specs shows deepseek/v4-pro at 80.6% swe-bench.

**verdict:** covered.

### usecase.4: choose model for context size

criteria says:
```
given('a developer needs to process large documents')
  then('they find deepseek/v4-pro with 512K context')
```

blueprint model specs shows deepseek/v4-pro at 512K.

**verdict:** covered.

### usecase.5: access model specs programmatically

criteria says:
```
given('a developer has instantiated a brain')
  when('they access brain.spec.cost.cash.input')
    then('they receive the per-token input cost')
```

blueprint shows all models have spec.cost.cash.input/output values.

**verdict:** covered.

### usecase.6: handle model updates

criteria says:
```
given('a model is removed from together.ai')
  then('the slug is removed from the type union')
```

blueprint uses type union = compile-time slug validation.

**verdict:** covered.

---

## gaps found

none.

## why it holds

every requirement is traceable:
- wish: 4 lines → all addressed
- vision: 10 models + 1 price update → all addressed
- criteria: 6 usecases → all addressed

no requirements skipped. no features left unimplemented.

