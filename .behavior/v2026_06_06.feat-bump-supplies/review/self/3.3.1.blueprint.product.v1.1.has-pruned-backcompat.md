# review.self: has-pruned-backcompat

## the question

did we add backwards compatibility that was not explicitly requested?

## analysis

### backwards compat concerns in this change

**concern 1: extant slug preservation**

did the wisher explicitly say to maintain extant slugs?
- no explicit request

is there evidence this backwards compat is needed?
- yes: slug removal would break consumer code
- extant consumers depend on slugs like `together/qwen3/coder-next`
- semver: slug removal is a break in contract

verdict: keep. this is standard semver practice, not assumed safety.

**concern 2: extant spec shape preservation**

did the wisher explicitly say to maintain spec shape?
- no explicit request

is there evidence this backwards compat is needed?
- yes: BrainSpec is the contract consumers depend on
- spec shape change would break `.spec.cost.cash.input` access patterns
- new models use identical spec structure

verdict: keep. we do not change the shape, we add entries.

### what we did NOT add

we did not add:
- migration utilities
- deprecation warnings
- version aliases
- fallback mechanisms
- compatibility shims

these would be yagni violations.

## conclusion

no backwards compat additions. we preserve extant behavior by:
1. not removeed extant slugs
2. not changed extant spec shapes

this is not "assumed safety" - it is standard additive change practice.

