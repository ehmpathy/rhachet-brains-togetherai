# review.self: has-consistent-conventions

## the question

do new names and patterns diverge from extant conventions?

## analysis

### slug name convention

extant pattern: `together/{family}/{variant}`

| extant | pattern |
|--------|---------|
| together/qwen3/coder-next | together/{family}/{variant} |
| together/llama3.3/70b | together/{family}/{size} |
| together/deepseek/r1 | together/{family}/{variant} |

new entries:
| new | follows pattern? |
|-----|------------------|
| together/lfm2/24b | yes: together/{family}/{size} |
| together/gpt-oss/20b | yes: together/{family}/{size} |
| together/qwen3.5/9b | yes: together/{family}/{size} |
| together/gemma4/31b | yes: together/{family}/{size} |
| together/minimax/m2.5 | yes: together/{family}/{variant} |
| together/minimax/m2.7 | yes: together/{family}/{variant} |
| together/qwen3.5/397b | yes: together/{family}/{size} |
| together/deepseek/v4-pro | yes: together/{family}/{variant} |
| together/kimi/k2.6 | yes: together/{family}/{variant} |
| together/glm/5.1 | yes: together/{family}/{variant} |

verdict: all slugs follow extant convention.

### description convention

extant pattern: `{model name} - {capability} ({context}K)`

| extant | pattern |
|--------|---------|
| "Qwen3-Coder-Next - optimized for agentic code (262K)" | {name} - {capability} ({context}) |

new entries follow same pattern:
- "DeepSeek-V4-Pro - frontier code model (512K)"
- "LFM2-24B - ultra-cheap inference (128K)"

verdict: consistent.

### spec structure convention

extant pattern:
```ts
spec: {
  cost: { cash: { input, output } },
  gain: { size: { context: { tokens } }, grades: { swe } },
}
```

all new entries use identical structure.

verdict: consistent.

## conclusion

zero convention divergences. all names, descriptions, and structures follow extant patterns.

