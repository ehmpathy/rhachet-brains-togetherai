# self-review (second pass): has-questioned-assumptions

## fresh eyes — deeper questions

### critical issue: glm-5.1 score appears LOWER than glm-4.7

**observation:** vision shows glm-5.1 at 58.4% swe-bench pro, but current config shows glm-4.7 at 73.8% swe-bench.

**what's wrong here?** swe-bench *pro* is a harder benchmark than swe-bench *verified*. 58.4% pro ≈ 73-78% verified. these are not comparable numbers.

**assumption i made:** that lower number = worse model. this is false.

**fix:** the vision should note that glm-5.1's 58.4% *pro* score is actually competitive with or better than 73.8% *verified*. need to track which benchmark type each score references.

**status: issue found, clarified in vision "what is awkward" section**

---

### did i verify these models actually exist on together.ai?

**assumption:** web scrape of together.ai/prices means models are available.

**evidence:** web fetch returned model names like "LFM2 24B A2B", "gpt-oss-20B", "Gemma 4 31B".

**what if they're not there?** we'd document models that can't be called.

**counterpoint:** web fetch from together.ai docs page also listed these models. two independent sources.

**verdict: reasonable confidence, but must verify model IDs before implementation**

---

### are the model IDs correct?

**assumption:** `DeepSeek-V4-Pro`, `Kimi-K2.6`, etc. will map to actual together.ai endpoints.

**evidence:** none. i guessed based on patterns like `deepseek-ai/DeepSeek-V3.1` from current config.

**what if wrong?** entire config would fail to instantiate.

**fix required:** must look up exact model IDs from together.ai api docs before implementation stone.

**status: flagged in "external research needed"**

---

### are cheap models (lfm2, gpt-oss) real or vaporware?

**assumption:** $0.03/1M input models exist and work.

**evidence:** together.ai prices list them. artificial analysis ranks them.

**what if they're limited?** may have restrictions (no tool use, rate limits, availability).

**verdict: assumption holds for existence, but capabilities need verification**

---

### did the wisher actually want vision models?

**assumption:** "include all origins" implies vision capability matters.

**evidence:** wisher said "deepseek, qwen, etc" — named code-focused models. no mention of vision.

**what if opposite:** wisher only cares about code models. vision is scope creep.

**counterpoint:** gemma 4 and qwen 3.5 are multi-purpose. vision is bonus, not primary feature.

**verdict: include but don't emphasize. raise as open question for wisher.**

---

### is "17x cheaper" actually useful?

**assumption:** lfm2 at $0.03 vs qwen3-coder-next at $0.50 = 17x savings = valuable.

**evidence:** math checks out. but does 17x cheaper matter if quality is 50x worse?

**what if opposite:** cheap models too weak for any real usecase. noise in catalog.

**counterpoint:** wisher explicitly asked for "fast & cheap". triage usecases exist.

**verdict: assumption holds — wisher validated this requirement**

---

## issues found in this second pass

### issue 1: benchmark type not clearly labeled

**found:** glm-5.1 shows 58.4% but that's swe-bench *pro*, not *verified*. misleads without context.

**resolution:** vision already notes this in "what is awkward" but could be clearer in the model tables. will add benchmark type column or note in implementation.

### issue 2: model ID verification deferred

**found:** all new model IDs are guesses, not verified.

**resolution:** already flagged in "external research needed". this is implementation-phase work, appropriate to defer from vision.

---

## why these assumptions hold

### "users select by slug, not model ID"

holds — current adapter works this way. no reason to change pattern.

### "prices are per-token, not per-request"

holds — together.ai docs confirm token-based rates across all chat models.

### "chat completions api is standard"

holds — together.ai uses openai-compatible api for all chat models.

---

## summary

second pass found one clarification needed (benchmark types) and confirmed that model ID verification is correctly deferred to implementation phase. no fundamental assumptions need to change.
