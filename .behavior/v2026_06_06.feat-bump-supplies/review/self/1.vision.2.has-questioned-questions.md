# self-review: has-questioned-questions

## triage of open questions

### from vision "questions to validate with wisher"

#### Q1: should we add ALL models from together.ai or curate a subset?

**can this be answered via logic now?**
yes. the wish says "research and append the latest models" with focus on "fast & cheap" and "big & best". this implies curation, not exhaustive catalog.

**resolution:** [answered] — curate by tier. add notable models per category, not every variant.

---

#### Q2: how to handle model version updates (v3.1 -> v4)?

**can this be answered via logic now?**
yes. current config keeps both. pattern: add new version, deprecate old in next major.

**resolution:** [answered] — keep old versions for backwards compat. deprecate via semver major.

---

#### Q3: what's the policy for models that are removed?

**does only the wisher know?**
partially. this is a product decision. but we can propose a reasonable default.

**proposed answer:** remove slug in next major version. changelog entry. compile error for consumers = explicit migration path.

**resolution:** [wisher] — propose default, ask wisher to confirm or override.

---

#### Q4: should vision capability be a first-class concern?

**can this be answered via logic now?**
yes. wisher didn't mention vision. it's incidental to code-focused models.

**resolution:** [answered] — include vision-capable models but don't add special code. treat as text models that happen to support images.

---

### from vision "external research needed"

#### R1: verify exact model IDs from together.ai api documentation

**can this be answered via extant docs?**
yes, but not now. requires api call or docs deep-dive.

**resolution:** [research] — to be answered in research phase before implementation.

---

#### R2: confirm tool use support for each new model

**can this be answered via extant docs?**
yes, but requires per-model verification.

**resolution:** [research] — to be answered in research phase.

---

#### R3: check if cached token prices apply to all models

**can this be answered via logic now?**
no. together.ai docs mention cache for some models (deepseek v4 pro) but not all.

**resolution:** [research] — to be answered in research phase. only set cache rates if confirmed.

---

#### R4: validate context window sizes against api limits

**can this be answered via extant docs?**
yes, but requires per-model verification.

**resolution:** [research] — to be answered in research phase.

---

### from vision "assumptions made"

#### A1: together.ai price pages are accurate as of june 2026

**can this be verified?**
yes, web fetch was done on 2026-06-06.

**resolution:** [answered] — verified via live fetch. prices snapshot-in-time by design.

---

#### A2: swe-bench scores from third-party benchmarks are reliable

**can this be verified?**
partially. multiple sources report similar scores.

**resolution:** [answered] — scores are directional guidance, not guarantees. document sources.

---

#### A3: all listed models support the chat completions api

**can this be verified now?**
no. requires per-model verification.

**resolution:** [research] — verify in research phase. only add models that use chat completions.

---

#### A4: tool use capability is consistent across model versions

**can this be verified now?**
no. requires per-model verification.

**resolution:** [research] — verify in research phase. set `tooluse: false` if not confirmed.

---

## summary of triage

| question/assumption | status | action |
|---------------------|--------|--------|
| curate vs exhaustive | [answered] | curate by tier |
| version update policy | [answered] | keep both, deprecate via semver |
| model removal policy | [wisher] | propose default, confirm with wisher |
| vision as first-class | [answered] | no special code |
| verify model IDs | [research] | research phase |
| verify tool use | [research] | research phase |
| verify cache prices | [research] | research phase |
| verify context sizes | [research] | research phase |
| verify chat api | [research] | research phase |
| price accuracy | [answered] | verified via live fetch |
| benchmark reliability | [answered] | directional guidance |

---

## updates needed to vision

the vision "open questions & assumptions" section should be updated to mark each item with its triage status. however, since this is a review document, I will note this as a clarification for the implementation phase rather than modify the vision now.

**action:** ensure research phase addresses all [research] items before implementation begins.
