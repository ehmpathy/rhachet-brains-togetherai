# self-review (third pass): has-questioned-questions

## re-read with fresh eyes

review of the question triage from pass 2. I need to verify:
1. are the [answered] items truly answered?
2. are the [research] items correctly deferred?
3. are the [wisher] items truly blockers?

---

## verification of [answered] items

### "curate by tier" — holds

**why it holds:** the wish explicitly says "fast & cheap" and "big & best" — these are tiers, not an exhaustive list. curation is the explicit scope.

---

### "keep both versions, deprecate via semver" — holds

**why it holds:** this follows standard npm semver practices. current config shows this pattern (deepseek v3.1 extant despite v4 release). no ambiguity.

---

### "vision: no special code" — holds

**why it holds:** wisher asked for "latest models" not "vision support". gemma 4 and qwen 3.5 support vision but we don't need to modify the adapter contract. standard text input works.

---

### "prices verified via live fetch" — holds

**why it holds:** web fetch executed on 2026-06-06 from together.ai/prices. prices documented with source links in vision.

---

### "benchmarks are directional guidance" — holds

**why it holds:** swe-bench scores vary by source and benchmark version (pro vs verified). scores help users rank models but are not contractual guarantees.

---

## verification of [research] items

### "verify model IDs" — correctly deferred

**why deferred:** requires together.ai api docs or test calls. not available from public web scrape. research phase work.

### "verify tool use" — correctly deferred

**why deferred:** per-model capability. some models mention "function call enabled" but not all. must verify before we set `tooluse: true`.

### "verify cache prices" — correctly deferred

**why deferred:** only deepseek v4 pro explicitly mentions cache discount ($0.20). other models may or may not support cache. research needed.

### "verify context sizes" — correctly deferred

**why deferred:** web scrape gave approximate sizes (262K, 512K). exact api limits may differ. verify before implementation.

### "verify chat api" — correctly deferred

**why deferred:** together.ai uses openai-compatible api for most models but exceptions may exist (e.g., specialized endpoints).

---

## verification of [wisher] items

### "model removal policy" — truly a wisher question

**why it's a wisher question:** this is a product-level decision about backwards compatibility. we can propose a default (remove in next major, compile error for consumers) but the wisher may have different preferences based on user impact.

**action:** the proposal is documented. wisher can accept or override.

---

## issues found in this third pass

**none.** all triage decisions hold upon re-examination.

---

## why the triage is complete

1. **all questions categorized:** 11 items total, each marked [answered], [research], or [wisher]
2. **answered items verified:** logic sound, no ambiguity
3. **research items appropriately deferred:** require external verification not available from current artifacts
4. **wisher items are genuine blockers:** product decisions that require owner input

---

## summary

the question triage from pass 2 holds. no changes needed. ready to mark stone as passed.
