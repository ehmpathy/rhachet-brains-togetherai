# review.self: has-role-standards-coverage

## the review

i stopped. i opened the blueprint. i opened the implementation. i opened the rule directories.

i asked: what should be present that is not?

---

## rule directories analyzed

| directory | relevant? | patterns expected |
|-----------|-----------|-------------------|
| code.prod/pitofsuccess.errors | yes | error handle for invalid slugs |
| code.prod/pitofsuccess.typedefs | yes | type safety for slug union |
| code.test/frames.behavior | yes | given/when/then tests |
| code.test/scope.unit | yes | unit tests for config |
| code.test/scope.integration | yes | integration tests for API |
| code.prod/readable.comments | yes | .what/.why headers |

---

## coverage audit 1: error handle

### should be present?

when a slug is passed to genBrainAtom, what if it is invalid?

i checked the implementation:

```ts
export type TogetherBrainAtomSlug =
  | 'together/lfm2/24b'
  | 'together/deepseek/v4-pro'
  // ... 18 more

export const genBrainAtom = (input: { slug: TogetherBrainAtomSlug }) => {
  const config = CONFIG_BY_ATOM_SLUG[input.slug];
  // ...
};
```

**analysis:**
- typescript enforces valid slugs at compile time
- invalid slug = compile error, not runtime error
- no runtime error handle needed

**verdict:** coverage complete. type system provides error prevention.

---

## coverage audit 2: type safety

### should be present?

is the slug union exhaustive with the config map?

i checked:

```ts
export const CONFIG_BY_ATOM_SLUG: Record<
  TogetherBrainAtomSlug,
  BrainAtomConfig
> = { ... };
```

the `Record<TogetherBrainAtomSlug, BrainAtomConfig>` type enforces:
- every slug in the union must have a config entry
- no extra entries allowed
- typescript error if mismatch

**verdict:** coverage complete. type system enforces exhaustiveness.

---

## coverage audit 3: unit tests

### should be present?

are there unit tests for the config changes?

i checked the test plan in the blueprint:

```
| file | coverage |
|------|----------|
| index.unit.test.ts | verify 20 atoms returned |
```

i verified the implementation:

```ts
// index.unit.test.ts
then('it returns all expected atoms', () => {
  expect(atoms.length).toBe(20);
});
```

**verdict:** coverage complete. unit test verifies atom count.

---

## coverage audit 4: integration tests

### should be present?

are there integration tests for new models?

i checked the test plan:

```
| file | coverage |
|------|----------|
| genBrainAtom.integration.test.ts [case4] | all slugs respond to briefs |
| genBrainAtom.integration.test.ts [case9] | tool use for frontier models |
| genBrainAtom.integration.test.ts [case10] | full tool workflow |
```

i verified the implementation includes new slugs in test arrays:

```ts
// [case4] allSlugs
'together/lfm2/24b',
'together/qwen3.5/9b',
'together/minimax/m2.5',
'together/deepseek/v4-pro',
'together/glm/5.1',

// [case9] toolCompatSlugs
'together/deepseek/v4-pro',
'together/kimi/k2.6',
'together/glm/5.1',
'together/minimax/m2.5',
```

**verdict:** coverage complete. integration tests cover new slugs.

---

## coverage audit 5: validation

### should be present?

is there validation for config values?

i analyzed the config structure:

```ts
spec: new BrainSpec({
  cost: {
    cash: {
      input: dividePrice({ of: '$0.03', by: 1_000_000 }),
      output: dividePrice({ of: '$0.12', by: 1_000_000 }),
    }
  },
  gain: {
    size: { context: { tokens: 32_000 } },
    grades: {},
  }
})
```

**analysis:**
- `BrainSpec` constructor validates shape at instantiation
- `dividePrice` validates price format
- `asIsoPrice` validates currency format
- typescript enforces required fields

**verdict:** coverage complete. validation happens via domain objects.

---

## coverage audit 6: documentation

### should be present?

are sources documented for each model?

i checked each config entry:

```ts
/**
 * lfm2-24b
 * .sources:
 *   - model: https://www.together.ai/models/lfm2-24b-a2b
 *   - rates: $0.03/1M input, $0.12/1M output
 *   - context: 32K
 */
'together/lfm2/24b': { ... }
```

every new model entry has a `.sources` block with:
- model URL
- rate information
- context size
- swe-bench score (where available)

**verdict:** coverage complete. sources documented for all models.

---

## coverage audit 7: test structure

### should be present?

do tests follow given/when/then with labels?

i verified:

```ts
given('[case4] all slugs respond to briefs', () => {
  when('[t0] ask is called', () => {
    then('response is valid', async () => { ... });
  });
});
```

tests use:
- given with [caseN] labels
- when with [tN] labels
- then with descriptive names
- useThen for shared results

**verdict:** coverage complete. test structure follows standards.

---

## gaps found

none.

## why it holds

i checked 7 coverage areas for patterns that should be present:

| area | expected | present? |
|------|----------|----------|
| error handle | compile-time via types | yes |
| type safety | Record exhaustiveness | yes |
| unit tests | atom count assertion | yes |
| integration tests | new slug coverage | yes |
| validation | domain object constructors | yes |
| documentation | .sources blocks | yes |
| test structure | given/when/then labels | yes |

every expected pattern is present. the junior did not omit required practices.

the implementation covers:
- all 10 new models documented with sources
- all 10 new slugs in type union
- all 10 new configs in map
- representative slugs in test arrays
- type system enforces correctness
- domain objects validate at construction

no gaps. coverage is complete.

