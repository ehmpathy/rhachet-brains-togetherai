# review.self: has-questioned-assumptions

## the question

what technical assumptions did i make without evidence?

## assumptions surfaced

### assumption 1: model IDs from together.ai docs are correct

**what i assumed:** model IDs like `deepseek-ai/DeepSeek-V4-Pro` are accurate
**evidence:** verified against together.ai serverless models docs
**what if wrong:** API calls would fail at runtime
**mitigation:** integration tests will catch wrong IDs in CI

### assumption 2: prices are current as of implementation date

**what i assumed:** prices from together.ai docs are accurate
**evidence:** prices sourced from together.ai rates page (june 2026)
**what if wrong:** cost calculations would be inaccurate
**mitigation:** prices will need periodic update; this is documented in vision

### assumption 3: context window sizes are accurate

**what i assumed:** context sizes (128K, 256K, 512K) are correct
**evidence:** sourced from together.ai docs and model announcements
**what if wrong:** requests that exceed real limit would fail
**mitigation:** together.ai API returns error if context exceeded

### assumption 4: swe-bench scores are comparable

**what i assumed:** swe-bench verified scores are comparable across models
**evidence:** all scores from swe-bench verified leaderboard
**what if wrong:** users might compare apples to oranges
**mitigation:** scores labeled as `swe-bench verified` in spec

### assumption 5: tool use capability is consistent

**what i assumed:** all models support same tool use format
**evidence:** together.ai uses openai-compatible API
**what if wrong:** some models might fail tool calls
**mitigation:** integration tests [case9] verifies tool use per model

## simpler approach check

**could this be simpler?**
- already minimal: data in config map
- no abstraction, no indirection
- direct from docs to code

**verdict:** this is the simplest approach.

## conclusion

5 assumptions surfaced. each has mitigation:
- model IDs: integration tests
- prices: documented need for updates
- context sizes: api error on exceed
- swe-bench: labeled in spec
- tool use: integration tests
