# review.self: has-pruned-backcompat

## the review

i paused. i re-read the blueprint line by line.

i asked: where might a junior have added backwards compat that wasn't needed?

---

## blueprint filediff tree

```
src/
├── contract/sdk/
│   ├── [~] index.ts              # add slugs to export
│   └── [~] index.unit.test.ts    # update count assertion
└── domain.operations/atom/
    ├── [~] BrainAtom.config.ts   # add config entries
    └── [~] genBrainAtom.integration.test.ts  # add slugs to arrays
```

**observation:** all changes are `[~] update`, no `[+] create` or `[-] delete`.

**what this means:** pure addition. no removals. no new files.

---

## backwards compat concern analysis

### question 1: did we add compat shims?

i searched the blueprint for compat mechanisms:

| mechanism | present? | evidence |
|-----------|----------|----------|
| version aliases | no | no `v3 → v4` mappings |
| deprecation markers | no | no `@deprecated` annotations |
| fallback logic | no | no `if (oldSlug) use(newSlug)` |
| adapter wrappers | no | no adapter functions |
| migration utilities | no | no migration scripts |

**verdict:** zero compat mechanisms. this is pure data addition.

### question 2: did we preserve items "to be safe"?

i examined what was preserved:

**extant slugs:** 10 slugs remain in type union.
- why preserved: wish says "append the latest models"
- "append" implies addition, not replacement
- this is not compat work - it is per the ask

**extant spec shape:** BrainSpec unchanged.
- why preserved: wish doesn't ask for spec changes
- new models use same shape
- this is not compat work - spec fits the need

**extant tests:** test arrays extended, not replaced.
- why preserved: tests verify extant + new models
- extension is correct approach for catalog growth
- this is not compat work - it's correct test practice

### question 3: should we have removed any item?

i questioned: should old models be removed?

**context:** wish says "append the latest models"
- doesn't say "replace" or "remove outdated"
- no guidance on deprecation policy

**evidence for removal:**
- none: no models flagged as deprecated
- no performance issues with larger catalog
- no maintenance burden from extant entries

**verdict:** no removal warranted. wish is additive.

---

## open questions for wisher

**question:** should any extant models be deprecated?

- `together/deepseek/r1` is now superseded by `together/deepseek/v4-pro`
- `together/qwen3/coder-next` may be superseded by `together/qwen3.5/397b`
- however: no explicit deprecation policy in wish

**recommendation:** keep all. flag deprecation as future work if desired.

---

## conclusion

review for backwards compat:

| item | compat added? | why |
|------|---------------|-----|
| compat shims | no | pure data addition |
| version aliases | no | no versioned slugs |
| fallback logic | no | no conditionals added |
| deprecation warnings | no | no models deprecated |

**no unnecessary compat work found.**

this is additive change per wish: "append the latest models."

