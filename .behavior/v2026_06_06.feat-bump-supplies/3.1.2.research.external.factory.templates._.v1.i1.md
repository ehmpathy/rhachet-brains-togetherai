# 3.1.2.research.external.factory.templates

## summary

**no new templates required.**

implementation follows extant `BrainAtom.config.ts` template pattern already established in repo.

---

## extant template pattern

```ts
'together/{family}/{model}': {
  model: '{api-model-id}',
  description: '{desc} ({context}K)',
  spec: new BrainSpec({
    cost: { time: {...}, cash: {...} },
    gain: { size: {...}, grades: {...}, cutoff, domain, skills },
  }),
},
```

---

## implementation status

10 new models added to extant template. verified:
- types ✓
- lint ✓
- format ✓
- unit tests ✓

