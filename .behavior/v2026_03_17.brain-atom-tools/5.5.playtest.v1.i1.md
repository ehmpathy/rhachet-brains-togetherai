# playtest: brain atom tool use

## prerequisites

- node 20+ installed
- pnpm installed
- dependencies installed (`pnpm install --frozen-lockfile`)
- together ai API key available (load via `source .agent/repo=.this/role=any/skills/use.apikeys.sh`)

## sandbox

all playtest operations use the test suite — no manual file operations needed.

---

## happy paths

### 1. tool invocation (case5)

**action**: run full test suite and observe case5 results

```bash
source .agent/repo=.this/role=any/skills/use.apikeys.sh && \
THOROUGH=true npm run test:integration -- genBrainAtom.integration.test.ts
```

**expected outcome**:
- case5 tests pass (5 assertions)
- output shows `given: [case5] tool invocation` section with all tests green
- assertions verified:
  - "result.calls.tools contains invocations"
  - "result.output is null"
  - "each invocation has exid, slug, input"
  - "invocation.input is typed per tool schema"

---

### 2. tool continuation (case6)

**action**: observe case6 results in same test run

**expected outcome**:
- case6 tests pass (5 assertions)
- output shows `given: [case6] tool continuation` section with all tests green
- assertions verified:
  - "first ask returns tool calls"
  - "second ask with tool executions succeeds"
  - "brain synthesizes final answer from tool results"
  - "episode.exchanges accumulates tool exchange"
  - "result.calls is null after final answer"

---

### 3. error signals (case7)

**action**: observe case7 results in same test run

**expected outcome**:
- case7 tests pass (2 assertions)
- output shows `given: [case7] error signals in tool execution` section with all tests green
- assertions verified:
  - "brain receives error context and responds" (error:constraint)
  - "brain handles system failure gracefully" (error:malfunction)

---

### 4. model compatibility (case9)

**action**: observe case9 results in same test run

**expected outcome**:
- case9 tests pass for all models
- output shows `given: [case9] tool use model compatibility` section with all tests green
- models tested: qwen3, deepseek, kimi, llama4, llama3.3, glm

---

## edgey paths

### tools plugged but brain answers directly

**behavior**: when model can answer without tools, it may return direct response

**expected**: model decides whether to use tools based on prompt

**verified by**: case5 tool invocation uses prompt that requires tool use

---

### kimi/k2 model flakiness

**behavior**: kimi/k2 model may return 503 or malformed JSON occasionally

**expected**: external API issue, not a code defect

**why acceptable**: retry mechanism handles transient failures. other models work reliably.

---

## pass/fail criteria

### ✓ pass if:

1. all 45 tests pass
2. case5 passes — tool invocation works
3. case6 passes — tool continuation works
4. case7 passes — error signals handled
5. case9 passes — model compatibility verified
6. types pass (`npm run test:types` exits 0)

### ✗ fail if:

1. case5 fails — core tool invocation broken
2. case6 fails — continuation flow broken
3. case7 fails — error signal path broken
4. types fail — type definitions incorrect

---

## quick verify (all tests)

```bash
source .agent/repo=.this/role=any/skills/use.apikeys.sh && \
npm run test:types && \
THOROUGH=true npm run test:integration -- genBrainAtom.integration.test.ts
```

**expected**: all 45 tests pass, types check passes.

---

## verification run log

last verified: 2026-03-17

```
Test Suites: 1 passed, 1 total
Tests:       45 passed, 45 total
Time:        78.469 s
```

all case5, case6, case7, case9 tests passed.

