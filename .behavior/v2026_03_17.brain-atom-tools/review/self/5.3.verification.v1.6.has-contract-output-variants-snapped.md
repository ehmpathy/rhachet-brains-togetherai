# self-review: has-contract-output-variants-snapped

## verification: brain atom tool use

reviewed: public contract snapshots
against: output variant coverage

---

## public contracts modified

**file:** `src/contract/sdk/index.ts`

exports:
- `getBrainAtomsByTogetherAI` — returns all brain atoms (unchanged)
- `genBrainAtom` — factory for brain atoms (behavior extended to support tools)

**internal modules added (not exported from sdk):**
- `src/infra/cast/castIntoTogetherToolDef.ts` — internal cast function
- `src/infra/cast/castFromTogetherToolCall.ts` — internal cast function
- `src/infra/cast/castIntoTogetherToolMessages.ts` — internal cast function

---

## snapshot assessment

### are snapshots needed?

| contract | user-visible? | output type | needs snapshot? |
|----------|--------------|-------------|-----------------|
| genBrainAtom | yes | BrainOutput | already covered by integration tests |
| castIntoTogetherToolDef | no (internal) | OpenAI.ChatCompletionTool | no |
| castFromTogetherToolCall | no (internal) | BrainPlugToolInvocation | no |
| castIntoTogetherToolMessages | no (internal) | OpenAI.ChatCompletionToolMessageParam[] | no |

### why genBrainAtom doesn't need new snapshots

the BrainOutput contract is already tested via integration tests:
- case5 tests `result.calls.tools` shape (invocation structure)
- case6 tests tool continuation output
- case7 tests error signal flow

snapshots for LLM output are not meaningful — output varies per call. the test assertions verify structure, not content.

---

## issue found and fixed

**issue:** original review incorrectly claimed cast functions were "exports added" to sdk/index.ts

**verification:**
- checked `src/contract/sdk/index.ts` — only exports `getBrainAtomsByTogetherAI` and `genBrainAtom`
- checked `grep -E "export.*cast(Into|From)Together"` — cast functions only exported from their own files
- confirmed cast functions are in `src/infra/cast/` (internal), not in sdk exports

**fix:** updated "exports added" section to accurately describe:
- sdk exports (unchanged + extended behavior)
- internal modules added (not exported from sdk)

---

## additional verification

**snapshot landscape:**
- `glob **/*.snap` in src/ returns no files (only node_modules)
- `grep -c "toMatchSnapshot"` in integration tests returns 0
- confirms repo uses assertions, not snapshots

**why no snapshots:**
- LLM output varies per call — snapshots would be flaky
- assertions verify structure (exid, slug, input fields) not content

---

## conclusion

no new snapshots needed.

rationale:
1. cast functions are internal — not user-visible contracts
2. genBrainAtom output is LLM-variable — snapshots would be flaky
3. integration tests verify structure via assertions

