# self-review: has-questioned-deletables

## blueprint: brain atom tool use

reviewed: `.behavior/v2026_03_17.brain-atom-tools/3.3.1.blueprint.product.v1.i1.md`

---

## component-by-component review

### cast functions — all three needed

| component | delete? | rationale |
|-----------|---------|-----------|
| castIntoTogetherToolDef.ts | no | converts rhachet tool definitions to openai format — required for api calls |
| castFromTogetherToolCall.ts | no | parses response tool_calls into rhachet invocations — required for output |
| castIntoTogetherToolMessages.ts | no | converts execution results back to messages — required for continuation |

**why they hold**: each serves a different direction in the data flow. a combined file would mix unrelated responsibilities.

---

### TPlugs generic — needed

| component | delete? | rationale |
|-----------|---------|-----------|
| TPlugs extends BrainPlugs | no | enables conditional prompt type (string | BrainPlugToolExecution[]) |

**why it holds**: without TPlugs generic, the prompt type cannot vary based on whether tools are plugged. this is inherited from rhachet's BrainOutput type design — the pattern matches xai implementation.

---

### test cases — all needed

| test | delete? | rationale |
|------|---------|-----------|
| case5: tool invocation | no | core functionality — verifies tools are invoked |
| case6: tool continuation | no | different flow — verifies continuation with tool results |
| case7: error signals | no | verifies error handle for constraint and malfunction signals |
| case8: structured output with tools | no | verifies json_schema + tools interaction (found issue here) |
| case9: model compatibility | no | verifies all models support tool invocation |

**why they hold**: each tests a distinct behavior from the blackbox criteria. no redundancy.

---

### sdk exports — DELETION IDENTIFIED

| component | delete? | rationale |
|-----------|---------|-----------|
| export cast functions in sdk/index.ts | **yes** | cast functions are internal infra — consumers use genBrainAtom, not casts |

**action taken**: remove `[~] index.ts (export cast functions)` from blueprint filediff tree.

the cast functions are implementation details. consumers should not depend on them directly. internal scope allows change without breakage of public api.

---

## summary

- **deleted**: sdk export of cast functions (internal detail, not needed by consumers)
- **retained**: all cast functions, TPlugs generic, all test cases

the simplest version that works: implement cast functions as internal infra, extend genBrainAtom with tool support, test all behaviors from criteria.
