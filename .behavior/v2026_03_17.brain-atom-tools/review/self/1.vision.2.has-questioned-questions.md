# self-review: 1.vision.has-questioned-questions

## open questions triaged

### question 1: what does "w/o output schema" mean?

**status**: [answered]

**how it was resolved**:
- rhachet BrainAtom contract REQUIRES `schema: { output }` — this is not optional
- "w/o output schema" refers to the together ai API parameter, not the rhachet contract
- w/ output schema = send `response_format: json_schema` to together
- w/o output schema = do NOT send `response_format` (free-form text, client-side parse)

**no action needed** — already resolved in vision.

---

### question 2: test scope

**status**: [answered]

**how it was resolved**:
- based on extant repo patterns:
  - integration tests: test genBrainAtom internals with real together ai API
  - acceptance tests: test the published SDK contract (public exports only)

**no action needed** — already resolved in vision.

---

### question 3: json_schema + tools work together?

**status**: [research]

**what needs validation**:
- can send both `response_format: json_schema` AND `tools` in same request?
- does model return either tool_calls OR structured output (not both)?

**how to validate**:
- add explicit integration test case
- test with qwen3-coder-next (primary test model)

**marked for**: execution phase tests

---

### question 4: all models support function call?

**status**: [research]

**what needs validation**:
- do qwen3, deepseek, llama3.3, kimi, glm all support tool use?
- our BrainSpec has `skills: { tooluse: true }` for all atoms

**how to validate**:
- add model compatibility test loop (like case4 in extant tests)
- test tool invocation across all configured models

**marked for**: execution phase tests

---

## conclusion

questions triaged:
- 2 questions [answered] — already resolved in vision
- 2 questions [research] — marked for validation in execution phase tests

no questions require wisher input — scope is clear from wish + xai pr reference.
