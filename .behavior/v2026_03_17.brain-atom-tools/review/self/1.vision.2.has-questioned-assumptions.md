# self-review: 1.vision.has-questioned-assumptions (iteration 2)

## second pass: deeper look at assumptions

after the first review, I took a step back to look at the vision with fresh eyes.

### additional assumption surfaced: test coverage must match xai exactly

**what do we assume here without evidence?**
- wish says "same tool use test coverage" — but does that mean identical test cases?

**what evidence supports this assumption?**
- xai pr #15 has specific test patterns: tool invocation, continuation, error signals
- wish explicitly references that PR as the template

**what if the opposite were true?**
- could write different tests that achieve same coverage goals
- would still validate the same behaviors

**did the wisher say this, or did we infer it?**
- wisher said "same tool use test coverage" — this is explicit

**verdict**: ✅ HOLDS — wisher explicitly wants xai test parity

---

### additional assumption surfaced: genBrainAtom signature changes

**what do we assume here without evidence?**
- current signature needs TPlugs generic like xai
- prompt type becomes conditional: `string | BrainPlugToolExecution[]`

**what evidence supports this assumption?**
- xai implementation shows this pattern
- rhachet types require it for proper type inference

**what if the opposite were true?**
- could use simpler types, lose type safety
- would diverge from rhachet contract

**did the wisher say this, or did we infer it?**
- inferred from xai pattern, but required by rhachet types

**verdict**: ✅ HOLDS — required for rhachet type compatibility

---

### additional assumption surfaced: cast functions are the right boundary

**what do we assume here without evidence?**
- castIntoTogetherToolDef, castFromTogetherToolCall, castIntoTogetherToolMessages are the right decomposition

**what evidence supports this assumption?**
- xai uses same decomposition
- separates provider-specific format from domain types
- enables unit tests of cast logic

**what if the opposite were true?**
- could inline all conversion in genBrainAtom
- harder to test, more coupled

**did the wisher say this, or did we infer it?**
- inferred from xai pattern, follows bounded-context principle

**verdict**: ✅ HOLDS — clean architecture, matches xai

---

## conclusion

second pass confirms initial assumptions. no new issues found.

key assumptions that hold:
- test coverage must match xai patterns (explicit in wish)
- genBrainAtom signature needs TPlugs generic (rhachet requirement)
- cast function decomposition is correct (clean architecture)

ready to proceed with the vision as written.
