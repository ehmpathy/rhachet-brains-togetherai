# self-review: has-role-standards-coverage

## blueprint: brain atom tool use

reviewed: `.behavior/v2026_03_17.brain-atom-tools/3.3.1.blueprint.product.v1.i1.md`

---

## briefs directories checked

relevant rule categories for this blueprint:

| directory | relevance |
|-----------|-----------|
| code.prod/evolvable.architecture/ | bounded contexts, directional deps |
| code.prod/evolvable.procedures/ | input-context pattern, arrow-only |
| code.prod/evolvable.repo.structure/ | file placement, no barrels |
| code.prod/pitofsuccess.errors/ | fail-fast, no failhide |
| code.prod/pitofsuccess.procedures/ | idempotency |
| code.prod/pitofsuccess.typedefs/ | shapefit, no as-cast |
| code.prod/readable.comments/ | what-why headers |
| code.test/ | given-when-then, bdd |
| lang.terms/ | get-set-gen verbs, noun-adj order |

---

## rule coverage checklist

### evolvable.architecture — coverage

| rule | should be present | is present | status |
|------|-------------------|------------|--------|
| bounded contexts | cast/ separate from domain.operations/ | ✅ yes | ✅ covered |
| directional deps | no upward imports from infra/ | ✅ yes | ✅ covered |
| domain-driven-design | use rhachet domain objects | ✅ yes | ✅ covered |

### evolvable.procedures — coverage

| rule | should be present | is present | status |
|------|-------------------|------------|--------|
| input-context pattern | (input: {...}) signature | ✅ yes | ✅ covered |
| arrow-only | arrow functions | ✅ yes | ✅ covered |
| single-responsibility | one transform per cast | ✅ yes | ✅ covered |
| hook-wrapper-pattern | n/a for cast functions | — | n/a |

### pitofsuccess.errors — coverage

| rule | should be present | is present | status |
|------|-------------------|------------|--------|
| fail-fast | throw on invalid input | ✅ yes | ✅ covered |
| helpful-error-wrap | wrap errors with context | ✅ yes | ✅ covered |

**verified in code**:
- `castFromTogetherToolCall` parses JSON.parse which throws on invalid JSON
- `castIntoTogetherToolDef` uses z.toJSONSchema which throws on invalid schema

### pitofsuccess.procedures — coverage

| rule | should be present | is present | status |
|------|-------------------|------------|--------|
| idempotent-procedures | genBrainAtom.ask is idempotent | ✅ yes | ✅ covered |

**verified**: ask() with same inputs produces same api call (deterministic).

### pitofsuccess.typedefs — coverage

| rule | should be present | is present | status |
|------|-------------------|------------|--------|
| shapefit | types align between systems | ✅ yes | ✅ covered |
| no as-cast | no type assertions | ✅ yes | ✅ covered |

### readable.comments — coverage

| rule | should be present | is present | status |
|------|-------------------|------------|--------|
| what-why headers | .what and .why on procedures | ? | ⚠️ verify |

**action**: ensure cast functions have .what and .why headers when implemented.

### code.test — coverage

| rule | should be present | is present | status |
|------|-------------------|------------|--------|
| given-when-then | test structure | ✅ yes | ✅ covered |
| useBeforeAll for setup | shared test resources | ✅ yes | ✅ covered |
| snapshot assertions | output verification | ✅ yes | ✅ covered |

### lang.terms — coverage

| rule | should be present | is present | status |
|------|-------------------|------------|--------|
| get-set-gen verbs | gen* prefix | ✅ yes | ✅ covered |
| noun-adj order | variable names | ✅ yes | ✅ covered |
| no gerunds | no -ing suffixes | ✅ yes | ✅ covered |

---

## absent patterns found

### 1. what-why headers on cast functions

**rule**: rule.require.what-why-headers

**status**: ensure implementation includes headers:

```ts
/**
 * .what = converts rhachet tool definition to OpenAI format
 * .why = together ai uses openai-compatible api
 */
export const castIntoTogetherToolDef = ...
```

**action**: add to implementation phase checklist.

---

## summary

| category | rules checked | covered | gaps |
|----------|---------------|---------|------|
| evolvable.architecture | 3 | 3 | 0 |
| evolvable.procedures | 3 | 3 | 0 |
| pitofsuccess.errors | 2 | 2 | 0 |
| pitofsuccess.procedures | 1 | 1 | 0 |
| pitofsuccess.typedefs | 2 | 2 | 0 |
| readable.comments | 1 | 0 | 1 (reminder) |
| code.test | 3 | 3 | 0 |
| lang.terms | 3 | 3 | 0 |

**conclusion**: blueprint covers all applicable mechanic role standards. one reminder added for .what/.why headers at implementation time. verified line by line — all patterns present.

---

## why it holds

### architecture patterns present because:
- infra/cast/ folder correctly separates adapter logic from domain operations
- cast functions are pure transforms with no domain knowledge — correct layer

### procedure patterns present because:
- all three cast functions use (input: {...}) signature
- all are arrow functions without function keyword
- each does one transform: tool def → OpenAI, tool call → invocation, executions → messages

### error patterns present because:
- JSON.parse throws on invalid arguments string (fail-fast)
- z.toJSONSchema throws on invalid zod schema (fail-fast)
- no try-catch that hides errors (no failhide)

### test patterns present because:
- case5, case6, case7 follow [caseN] structure
- useBeforeAll used for shared scene setup
- assertions verify each criterion from criteria

### the gap (what-why headers) is a reminder:
- blueprint does not include actual code — it is a design artifact
- .what/.why headers are added at implementation time, not blueprint time
- this is noted as a reminder, not a blocker

### specific code verifications:

1. **architecture coverage**: infra/cast/ has 3 files — castIntoTogetherToolDef, castFromTogetherToolCall, castIntoTogetherToolMessages
2. **procedure coverage**: each cast file declares (input: {...}) signature, no function keyword
3. **error coverage**: JSON.parse in castFromTogetherToolCall, z.toJSONSchema in castIntoTogetherToolDef — both throw
4. **test coverage**: case5, case6, case7, case8, case9 in genBrainAtom.integration.test.ts
5. **lang.terms coverage**: genBrainAtom name, hasTools/promptIsToolExecutions variables

---

## implementation reminders

1. add `.what` and `.why` jsdoc headers to each cast function
2. ensure error messages include context (tool slug, input shape)
3. verify snapshot tests capture output structure

