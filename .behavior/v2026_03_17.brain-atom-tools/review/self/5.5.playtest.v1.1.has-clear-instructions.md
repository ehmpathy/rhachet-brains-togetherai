# self-review: has-clear-instructions

## review: playtest instructions

reviewed: playtest clarity
against: foreman followability

---

## checklist

### can foreman follow without prior context?

**yes** — playtest is self-contained:
- prerequisites list all requirements (node, pnpm, api keys)
- single test command runs all relevant tests
- expected outcomes reference specific test case names
- no assumptions about prior knowledge

### are commands copy-pasteable?

**yes** — verified by actual execution:
- command runs without modification
- api key sourced via documented skill
- THOROUGH=true included to run all tests
- test file path explicit

```bash
source .agent/repo=.this/role=any/skills/use.apikeys.sh && \
THOROUGH=true npm run test:integration -- genBrainAtom.integration.test.ts
```

tested and confirmed: 45 tests pass

### are expected outcomes explicit?

**yes** — each step lists:
- number of assertions expected
- specific test case section to observe
- exact assertion messages to verify
- pass/fail criteria clearly stated

---

## issue found and fixed

**issue:** original playtest used `--testNamePattern` flag that failed to find tests

**verification:**
```
npm run test:integration -- --testNamePattern="case5"
# result: "No tests found"
```

**fix:** simplified commands to use direct file path instead of pattern match

```bash
# before (broken)
npm run test:integration -- --testPathPattern="genBrainAtom" --testNamePattern="case5.*t0"

# after (works)
npm run test:integration -- genBrainAtom.integration.test.ts
```

---

## additional verification

### types check pass

```sh
npm run test:types
# exit code 0 — no type errors
```

### full test run verified

```
Test Suites: 1 passed, 1 total
Tests:       45 passed, 45 total
```

---

## conclusion

instructions are followable. commands are copy-pasteable and verified. expected outcomes are explicit.
