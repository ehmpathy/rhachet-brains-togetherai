# self-review: has-consistent-conventions

## execution: brain atom tool use

reviewed: `.behavior/v2026_03_17.brain-atom-tools/5.1.execution.phase0_to_phaseN.v1.i1.md`

---

## convention checklist

### file names

| new file | extant pattern | consistent? | evidence |
|----------|---------------|-------------|----------|
| castIntoTogetherToolDef.ts | castInto* prefix | yes | matches xai repo: castIntoXaiToolDef |
| castFromTogetherToolCall.ts | castFrom* prefix | yes | matches xai repo: castFromXaiToolCall |
| castIntoTogetherToolMessages.ts | castInto* prefix | yes | matches xai repo: castIntoXaiToolMessages |

### function names

| new function | extant pattern | consistent? |
|--------------|---------------|-------------|
| castIntoTogetherToolDef | castInto{Provider}{What} | yes |
| castFromTogetherToolCall | castFrom{Provider}{What} | yes |
| castIntoTogetherToolMessages | castInto{Provider}{What}s | yes |

### variable names

| variable | extant pattern | consistent? |
|----------|---------------|-------------|
| hasTools | has* boolean prefix | yes |
| promptIsToolExecutions | *Is* boolean infix | yes |
| toolsConfig | *Config suffix | yes |
| invocationsFromResponse | *From* source infix | yes |

### type annotations

| pattern | extant convention | consistent? |
|---------|------------------|-------------|
| TPlugs generic | T* prefix for type params | yes |
| BrainPlugToolInvocation | BrainPlug* prefix | yes (from rhachet) |
| BrainPlugToolExecution | BrainPlug* prefix | yes (from rhachet) |

---

## divergences found

**none.** all names follow extant codebase conventions.

---

## why it holds

- **cast* prefix**: all three cast functions use `castInto` or `castFrom` prefix — matches xai repo pattern
- **Together provider name**: uses `Together` in function names — consistent with repo name `rhachet-brains-togetherai`
- **variable booleans**: `hasTools`, `promptIsToolExecutions` follow extant `has*` and `*Is*` patterns
- **type params**: `TPlugs` follows extant `T*` convention for generics

### specific verifications

1. **genBrainAtom.ts line ~45**: `TPlugs extends BrainPlugs` — T prefix for generic
2. **genBrainAtom.ts line ~80**: `const hasTools = ...` — has* prefix for boolean
3. **castIntoTogetherToolDef.ts**: function name matches `castInto{Provider}{What}` pattern
4. **castFromTogetherToolCall.ts**: function name matches `castFrom{Provider}{What}` pattern
5. **test file**: case5, case6, case7 follow extant case number sequence — no gaps

**conclusion**: all conventions consistent with extant codebase. no divergent names or patterns. verified line by line.
