# self-review: has-pruned-yagni

## blueprint: brain atom tool use

reviewed: `.behavior/v2026_03_17.brain-atom-tools/3.3.1.blueprint.product.v1.i1.md`
against: `.behavior/v2026_03_17.brain-atom-tools/1.vision.md`
against: `.behavior/v2026_03_17.brain-atom-tools/2.1.criteria.blackbox.md`

---

## what was explicitly requested?

from vision usecases:
1. tool invocation with output schema
2. tool invocation without output schema (no response_format)
3. tool continuation — execute tools, feed results back

from blackbox criteria:
- usecase.1: tool invocation
- usecase.2: tool continuation
- usecase.3: structured output with tools
- usecase.4: type safety
- usecase.5: metrics and observability

---

## blueprint component review

### cast functions — requested?

| component | requested by | verdict |
|-----------|-------------|---------|
| castIntoTogetherToolDef | usecase.4 (type safety via zod conversion) | ✅ needed |
| castFromTogetherToolCall | usecase.1 (tool invocation output) | ✅ needed |
| castIntoTogetherToolMessages | usecase.2 (tool continuation) | ✅ needed |

**why it holds**: each cast function maps directly to a usecase. no extras.

---

### genBrainAtom extension — requested?

| component | requested by | verdict |
|-----------|-------------|---------|
| TPlugs generic | usecase.4 (type safety) | ✅ needed |
| conditional prompt type | usecase.2 (tool continuation) | ✅ needed |
| tools in api call | usecase.1 (tool invocation) | ✅ needed |
| tool_calls parse | usecase.1 (tool invocation) | ✅ needed |
| calls property in output | usecase.1 (tool invocation) | ✅ needed |

**why it holds**: each extension maps to criteria. no "future flexibility" abstractions.

---

### test coverage — requested?

| test case | requested by | verdict |
|-----------|-------------|---------|
| case5: tool invocation | usecase.1 | ✅ needed |
| case6: tool continuation | usecase.2 | ✅ needed |
| case7: error signals | usecase.2 (error:constraint, error:malfunction) | ✅ needed |
| case8: structured output | usecase.3 | ✅ needed |
| case9: model compatibility | vision: all models work | ✅ needed |

**why it holds**: each test case verifies a criterion. no "nice to have" tests.

---

### sdk exports — requested? — YAGNI FOUND

| component | requested by | verdict |
|-----------|-------------|---------|
| export cast functions in sdk | NOT REQUESTED | ⚠️ YAGNI |

**action taken**: removed from blueprint in deletables review.

cast functions are internal infra. consumers use genBrainAtom, not casts. export adds api surface without benefit.

---

## "while we're here" check

did we add extra items "while we're here"?

| potential extra | analysis | verdict |
|-----------------|----------|---------|
| parallel tool call tests | not in criteria, together supports by default | not added (correct) |
| tool_choice parameter variations | not in criteria | not added (correct) |
| stream with tools | not in criteria | not added (correct) |
| tool timeout handle | not in criteria | not added (correct) |

**no extras found**. blueprint is minimum viable.

---

## "future flexibility" check

did we add abstraction for future?

| potential over-build | analysis | verdict |
|---------------------|----------|---------|
| generic cast factory | could have made extensible cast system | not added (correct) |
| plugin architecture for tools | could have made tool plugins | not added (correct) |
| custom tool_choice options | could have exposed tool_choice param | not added (correct) |

**no over-build found**. cast functions are direct and specific.

---

## summary

| component | status |
|-----------|--------|
| cast functions | ✅ needed — map to usecases |
| genBrainAtom extension | ✅ needed — map to criteria |
| test coverage | ✅ needed — verify criteria |
| sdk exports | ⚠️ YAGNI — removed |
| parallel tool tests | ✅ correctly omitted |
| stream with tools | ✅ correctly omitted |

**conclusion**: blueprint is minimum viable for the wish. only YAGNI item (sdk exports) already pruned.
