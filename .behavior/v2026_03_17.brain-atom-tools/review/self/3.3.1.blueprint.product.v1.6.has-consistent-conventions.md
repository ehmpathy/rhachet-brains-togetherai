# self-review: has-consistent-conventions

## blueprint: brain atom tool use

reviewed: `.behavior/v2026_03_17.brain-atom-tools/3.3.1.blueprint.product.v1.i1.md`
verified against: `src/infra/cast/*.ts`, `src/domain.operations/atom/genBrainAtom.ts`
reference: rhachet-brains-xai PR #15

---

## file name conventions

### cast function files

| file | xai pattern | together pattern | consistent? |
|------|-------------|------------------|-------------|
| castIntoXaiToolDef.ts | `castInto{Provider}ToolDef` | castIntoTogetherToolDef.ts | ✅ yes |
| castFromXaiToolCall.ts | `castFrom{Provider}ToolCall` | castFromTogetherToolCall.ts | ✅ yes |
| castIntoXaiToolMessages.ts | `castInto{Provider}ToolMessages` | castIntoTogetherToolMessages.ts | ✅ yes |

**verdict**: file names follow `cast{Into|From}{Provider}{Type}.ts` convention from xai.

---

## function name conventions

### cast functions — verb prefix pattern

| function | verb | direction | matches pattern? |
|----------|------|-----------|-----------------|
| castIntoTogetherToolDef | cast | into provider | ✅ `castInto*` |
| castFromTogetherToolCall | cast | from provider | ✅ `castFrom*` |
| castIntoTogetherToolMessages | cast | into provider | ✅ `castInto*` |

**verdict**: function names use `cast{Into|From}` prefix per direction.

---

## directory structure conventions

### src/ tree

```
src/
├─ contract/sdk/        # public exports
├─ domain.operations/   # business logic
│    └─ atom/
└─ infra/               # infrastructure adapters
     └─ cast/           # type transformation functions
```

**verified**:
- cast functions in `infra/cast/` — correct, these are infrastructure adapters
- genBrainAtom in `domain.operations/atom/` — correct, this is domain logic
- sdk exports in `contract/sdk/` — correct, public api surface

**verdict**: directory placement follows bounded context pattern.

---

## type convention

### generic parameter names

| generic | location | purpose | conventional? |
|---------|----------|---------|---------------|
| TOutput | genBrainAtom.ts:59 | output schema type | ✅ standard `T` prefix |
| TPlugs | genBrainAtom.ts:59 | plugs type parameter | ✅ standard `T` prefix |

### type constraints

| constraint | location | pattern |
|------------|----------|---------|
| `TPlugs extends BrainPlugs = BrainPlugs` | line 59 | ✅ bounded with default |
| `TOutput` | line 59 | ✅ inferred from schema |

**verdict**: generic parameters follow typescript conventions.

---

## variable name conventions

### noun-adjective order (per rule.require.order.noun_adj)

verified in genBrainAtom.ts:

| variable | order | conventional? |
|----------|-------|---------------|
| `promptIsToolExecutions` | noun + state | ✅ correct |
| `hasTools` | noun + state | ✅ correct |
| `wantStructuredOutput` | verb + noun | ✅ correct (intent) |
| `tools` | noun | ✅ correct |
| `toolDefs` | noun + qualifier | ✅ correct |

**verdict**: variables follow noun-state order convention.

---

## import conventions

### cast function imports in genBrainAtom.ts

```ts
// lines 19-21
import { castFromTogetherToolCall } from '../../infra/cast/castFromTogetherToolCall';
import { castIntoTogetherToolDef } from '../../infra/cast/castIntoTogetherToolDef';
import { castIntoTogetherToolMessages } from '../../infra/cast/castIntoTogetherToolMessages';
```

**verified**:
- relative path from domain.operations to infra ✅
- no barrel exports (direct file import) ✅
- alphabetical order ✅

**verdict**: imports follow directional deps (domain → infra) and avoid barrel exports.

---

## summary

| convention area | status | notes |
|-----------------|--------|-------|
| file names | ✅ consistent | follows xai cast* pattern |
| function names | ✅ consistent | cast{Into|From} prefix |
| directory structure | ✅ consistent | bounded contexts respected |
| type parameters | ✅ consistent | T* prefix with bounds |
| variable names | ✅ consistent | noun-state order |
| import paths | ✅ consistent | directional, no barrels |

**conclusion**: all conventions match established patterns from xai reference and repo briefs.

---

## lessons from this review

1. **xai pr #15 as reference**: the cast function name and file patterns established there carry over exactly.

2. **infra/ for adapters**: cast functions transform between rhachet types and provider types — correct placement in infra.

3. **no barrel exports**: each cast function imported directly by file path, not via index.ts.

4. **directional deps respected**: domain.operations imports from infra, never the reverse.

