# self-review: has-pruned-backcompat

## execution: brain atom tool use

reviewed: `.behavior/v2026_03_17.brain-atom-tools/5.1.execution.phase0_to_phaseN.v1.i1.md`

---

## backwards compatibility checklist

### API changes

| change | backwards compatible? | explicitly requested? | status |
|--------|----------------------|----------------------|--------|
| TPlugs generic added | yes — defaults to BrainPlugs | no — natural design | ✅ safe |
| prompt type union | yes — string still works | noted in blueprint | ✅ safe |
| output nullable | yes — only when tools plugged | yes — criteria | ✅ required |
| calls property added | yes — null when no tools | yes — criteria | ✅ required |

### extant behavior preserved

| behavior | still works? | evidence |
|----------|-------------|----------|
| ask without tools | yes | case1-4 tests pass |
| structured output | yes | case3 tests pass |
| episode continuation | yes | case6 tests pass |
| metrics tracked | yes | extant tests pass |

---

## unneeded backcompat found

**none.** all backwards compatibility is either:
1. natural from additive changes (new generic defaults to old type)
2. explicitly requested in criteria (output nullable when tools called)

---

## why it holds

- no hacks to preserve old behavior that no longer matters
- no deprecated code paths kept "just in case"
- changes are additive — new feature, old callers unaffected
- prompt type union is natural extension, not backcompat hack
- TPlugs defaults preserve call sites without explicit plugs

---

## summary

### specific code verifications

1. **genBrainAtom.ts line ~120**: TPlugs defaults to BrainPlugs — old callers work unchanged
2. **genBrainAtom.ts line ~150**: Array.isArray(prompt) check — string prompts still work
3. **genBrainAtom.ts line ~180**: calls property is null when no tools — old contract unchanged
4. **test case1-4**: all pass — proves extant behavior preserved

**conclusion**: no unneeded backwards compatibility code. all compat is either natural from additive design or explicitly required by criteria. verified line by line.
