# self-review: behavior-declaration-adherance

## execution: brain atom tool use

reviewed: `.behavior/v2026_03_17.brain-atom-tools/5.1.execution.phase0_to_phaseN.v1.i1.md`
against: vision, criteria, blueprint

---

## vision adherance checklist

| vision statement | implementation matches? | verification |
|-----------------|------------------------|--------------|
| "plugs: { tools }" syntax | yes | exact param name used in genBrainAtom |
| "result.calls.tools" shape | yes | returns BrainPlugToolInvocation[] as specified |
| "output is null when tools called" | yes | conditional return logic matches spec |
| "loop while result.calls" | yes | calls property null when output present |
| "prompt: executions" for continuation | yes | Array.isArray(prompt) branch handles this |
| "episode.exchanges accumulates" | yes | extant episode logic reused unchanged |

---

## criteria adherance checklist

### usecase.1 adherance

| criterion statement | matches spec? | how |
|---------------------|---------------|-----|
| "result.calls.tools contains invocations" | yes | array of BrainPlugToolInvocation |
| "result.output is null when tools called" | yes | output null, calls present |
| "each invocation has exid, slug, input" | yes | all three fields extracted |
| "invocation.input typed per tool schema" | yes | generics preserve types |

### usecase.2 adherance

| criterion statement | matches spec? | how |
|---------------------|---------------|-----|
| "BrainPlugToolExecution[] as prompt" | yes | Array.isArray check |
| "episode.exchanges accumulates" | yes | extant accumulation logic |
| "signal: 'success'" | yes | cast function handles |
| "signal: 'error:constraint'" | yes | cast function handles |
| "signal: 'error:malfunction'" | yes | cast function handles |

### usecase.3 adherance

| criterion statement | matches spec? | how |
|---------------------|---------------|-----|
| "response_format json_schema sent" | yes | conditional on schema presence |
| "result.output matches schema" | yes | zod parse validates |

---

## blueprint adherance checklist

### cast functions match blueprint

| blueprint spec | implementation | adherance |
|----------------|----------------|-----------|
| castIntoTogetherToolDef | converts BrainPlugToolDefinition → OpenAI tool | yes |
| castFromTogetherToolCall | extracts id, function.name, arguments | yes |
| castIntoTogetherToolMessages | maps signal to tool message | yes |

### genBrainAtom matches blueprint

| blueprint spec | implementation | adherance |
|----------------|----------------|-----------|
| TPlugs generic | TPlugs extends BrainPlugs = BrainPlugs | yes |
| tool_calls detection | checks response.choices[0].message.tool_calls | yes |
| Array.isArray check | if (Array.isArray(prompt)) branch | yes |
| conditional response_format | only sent when hasTools && schema | yes |

### test cases match blueprint

| blueprint spec | test implementation | adherance |
|----------------|---------------------|-----------|
| case5 tool invocation | tests tool request and direct answer | yes |
| case6 tool continuation | tests ask → execute → continue → output | yes |
| case7 error signals | tests constraint and malfunction | yes |
| case8 structured output | tests response_format + tools | yes |
| case9 model compatibility | tests across model slugs | yes |

---

## deviations found

**none.** implementation matches specification exactly.

---

## why it holds

### vision adherance holds because

the code uses exact terminology from the vision:
- `plugs: { tools }` — not `options.tools` or `config.tools`
- `result.calls.tools` — not `result.toolCalls` or `result.invocations`
- `result.output` — nullable when tools present
- episode continuation via prompt array — exactly as vision describes

### criteria adherance holds because

each criterion maps to a test assertion:
- usecase.1 criteria → case5 assertions
- usecase.2 criteria → case6 and case7 assertions
- usecase.3 criteria → case8 assertions
- usecase.4 and .5 — implicit in type system and extant metrics

### blueprint adherance holds because

each file and codepath matches the blueprint:
- cast function names match exactly
- cast function signatures match exactly
- genBrainAtom changes are additive (no rewrites)
- test case numbers and scopes match exactly

### specific code verifications

| spec | code | match? |
|------|------|--------|
| "exid from id" | `exid: toolCall.id` | yes |
| "slug from function.name" | `slug: toolCall.function.name` | yes |
| "input from JSON.parse" | `input: JSON.parse(toolCall.function.arguments)` | yes |
| "signal mapped to content" | switch on execution.signal | yes |
| "TPlugs generic defaults" | `TPlugs extends BrainPlugs = BrainPlugs` | yes |

**conclusion**: implementation adheres to behavior declaration. no deviations from vision, criteria, or blueprint. verified line by line against specification.
